---
title: AKM Index v1.2 자가진단 — D.A.E (Demian AI-agent Ecosystem)
project: 00 Skills / cmdspace-hub
phase: 6-VERIFY
owner: Max (채점·Kant 렌즈) · 증거 수집 Explore 에이전트
status: review
created: 2026-09-22
updated: 2026-09-22
version: 1.0
rubric: AKM Index v1.2 (akm.cmdspace.work · 30-akm-index/akm-index-latest-kr.md)
submission: 로컬 전용 — CMDSPACE 서버 미전송 (consent.submit=false)
---

# AKM Index 자가진단 — D.A.E

> **총점 74.5 / 100 · 밴드 M3 오케스트레이션** (61~80: 멀티 에이전트·훅·루프가 실제 가동, 강제 장치 있음)
> 평가 도중 한 가지를 즉시 고쳤습니다(§5). 공식 점수는 **조치 전 상태**로 매깁니다(루브릭 §8.4 "평가 직전 벼락치기" 방지). 조치를 반영하면 **75.5** 예상입니다.

AKM Index 기반 자가평가입니다. 평가자와 피평가자가 같아 이해충돌이 있으므로, 루브릭 §8.4에 따라 **기준마다 감점 사유를 1개 이상** 적었고, 두 레벨 사이에서 애매하면 낮은 쪽을 택했습니다. 점수는 모두 `[추정]`입니다. 증거 경로는 수집 에이전트가 실존을 확인했습니다.

---

## 0. 시스템 경계 선언 (§8.1)

| 포함 | 경로 |
|---|---|
| 에이전트 설정 | `C:/Users/user/.claude/` — 룰 25, 훅 스크립트 25(settings 훅 37개·이벤트 14종), 에이전트 54, 스킬 2,529(자작·큐레이션 233은 `applied-skills.json`) |
| 생산 작업공간 | `D:/00 Antigravity/` — `00 Operation`(작업 큐·자동화) · `00 Skills`(스킬 SSOT·크로스AI 팩) · 프로젝트 128개 |
| 지식 기반 | `D:/00 Obsidian/Obsidian/` (볼트, git 61커밋) |
| 보조 런타임 | Eva(`~/.gemini/GEMINI.md`) · Emil/Codex(`~/.codex/AGENTS.md`·agents 32) |
| 자동화 호스트 | 이 PC(HP Victus) 1대 — 예약 작업 46개 중 D.A.E 계열 30여 개 |

**미접근 노드:** M2 MacBook(서브 기기)은 이번 세션에서 열지 못했습니다. 대체 입증 없이 **X2에서 불리하게 반영**했습니다.

---

## 1. 점수표

배점은 P·H·L 기준당 4.0, C 5.0, X 3.0이며, 환산점수 = 레벨 ÷ 4 × 배점입니다.

### P — Prompt Engineering (20%) · **16.0 / 20**

| 기준 | 레벨 | 근거 (1줄) | 감점 사유 (1줄) |
|---|---|---|---|
| P1 지시 계층화 | **3** | 전역 룰 25 ↔ 프로젝트 CLAUDE.md(Antigravity·볼트) ↔ 스킬 분리. 2026-08-11 룰 간 모순 교정 기록 | 중복·충돌 점검이 **1회성 분석**이고 정기 감사 절차가 없음. 룰 25개(약 68k 토큰)를 매 세션 통째로 로드 |
| P2 작업 계약 | **4** | `dgrs-gate.js`가 `deck_audit.py` 실행을 강제(불합격 시 exit 1). slide-guidelines v2.8에 "반복 지적 원인 6 → 게이트 개정" 기록 | 기계 검증이 **덱 산출물에만** 있음. 제안서·교재·노션은 문서 규칙에 의존 |
| P3 트리거·라우팅 | **4** | `route-agents.js`에 상호배타 매처(negative 조건)와 테스트. "등재 25개 중 19개 호출 0회" 실측 → v2.0 실패 확인 → 훅 이관 → 09-18 재개정 | 라우팅 로그 1,600건 중 **hits=0이 752건(47%)**인데 집계·개정에 쓰인 적 없음 |
| P4 예시·반례 | **3** | `humanize-korean` 분류학 + 승격 게이트(재현 2회·90일), GR Before/After 예시, BananaX 301개 채점본 목표 등재 | **정훈님 본인 발행물 문체 코퍼스가 없음.** 새 산출물이 코퍼스로 자동 편입되는 절차 없음 |
| P5 자산 수명주기 | **2** | 룰 18/25에 버전·HISTORY. SSOT `applied-skills.json` → 팩 자동 생성. 은퇴 에이전트 폴더(`_retired_20260907`) | **`~/.claude`가 VCS 밖**(평가 시점). 이력은 `.bak` 파일뿐. 룰 7개는 버전·HISTORY 모두 없음 |

### C — Context Engineering (25%) · **17.5 / 25**

| 기준 | 레벨 | 근거 | 감점 사유 |
|---|---|---|---|
| C1 주소체계 | **2** | 볼트 CLAUDE.md에 LLM-Wiki 7폴더 역할표. `obsidian-antigravity-ops.md` 생산↔기억 헌법. SYSTEM-MAP.html | 볼트 루트에 **PARA 계열 8개와 LLM-Wiki 계열 7개가 병존**하는데 지도는 한쪽만 설명. 루트에 `무제*.canvas` 4개·빈 노트. 07-30 경로 오타로 볼트 검색이 조용히 실패한 전례 |
| C2 메타데이터 | **3** | 볼트 frontmatter 표준(`origin`·`confirmed`), Antigravity 산출물 frontmatter 관례, `publish_to_vault.py`가 `source`·`synced` 자동 스탬프 | `/lint`가 **정기 실행되지 않음**(스케줄·실행 로그 없음) |
| C3 검색 인프라 | **2** | Smart Connections 임베딩(3,274 항목, 오늘 갱신) | 시맨틱 인덱스가 **Obsidian 앱 안에만** 있고 에이전트용 MCP/CLI 경로가 없음. `qmd` 스킬은 있으나 바이너리 미설치. Antigravity 쪽은 grep뿐 |
| C4 점진 공개 | **3** | active.md(큐) → DASHBOARD(지도) → current_state(상세) 계층. `session-start.js`가 당일 Work List만 주입. 08-28 토큰 예산 근거로 라우팅을 훅으로 이관 | **정작 인덱스가 비대함** — active.md 46.7KB, 00 Operation current_state 78.7KB. LLM용 증류 문서(`context/`)는 06-06 이후 갱신 없음 |
| C5 출처·신선도 | **4** | `publish_to_vault.py` 해시 기반 파생물 갱신. 크로스AI 팩 3일 주기 자동 재생성(`auto_generated: true`). `sync_tasks.py` 주간 stale 탐지(09-21 로그, 128개 프로젝트) | 볼트 루트에 `current-state.md`와 `current_state.md` **이름 중복** |

### H — Harness Engineering (20%) · **16.0 / 20**

| 기준 | 레벨 | 근거 | 감점 사유 |
|---|---|---|---|
| H1 도구 표면 | **3** | Claudian(볼트 상주·파일/bash), `obsidian-cli` 스킬, `/ingest`·`/query`·`/lint`, 자작 스크립트(notion_md_backup·publish_to_vault·tg_send…) + 테스트 9개 | 볼트 전용 MCP 없음. 도구 사용 통계 없음 |
| H2 스킬 라이브러리 | **3** | 도메인 스킬 233(SSOT) + 메타 스킬(skill-creator 등). 결과를 정위치에 되씀 | **스킬 폴더 2,529개 중 대부분이 일괄 설치 잡음**(2026-02-24). 스킬별 사용 빈도·성공률 기록 없음 |
| H3 가드레일 | **3** | `guard-destructive.js`가 비가역 4종을 차단(이번 평가 중에도 `rm -rf` 1건 실제 차단). 사고 2건 → 가드 신설(07-30 Drive 손상, 08-17 키 노출 → `scan_exposed_keys.ps1`) | `dangerouslySkipPermissions: true`에 deny 목록 0개. **정기 보안 점검 스케줄 없음** → 4점 조건 미충족 |
| H4 지속 메모리 | **3** | 프로젝트별 구조화 메모리(인덱스 + 항목별 파일, 타입 구분). 핸드오프 3중(current_state·Work List·session-start 주입). 09-19 메모리 정리 백업 흔적 | 메모리 히트율·스테일 점검 기록 없음. 볼트 프로젝트 메모리는 2개로 얇음 |
| H5 오케스트레이션 | **4** | 역할 분화 에이전트 54, 예약 작업 30여 개, DAIOS 세션 분석 리포트 99개, `AUTOMATION-INVENTORY.md`(09-06)에서 죽은 작업 3건 발견 → 2건 복구 | **예약 작업 7개가 한 번도 실행되지 않음**(FLOW-Jarvis·Secretary 계열, R=267011). FLOW-CustomerAlert 최근 결과 R=3 |

### L — Loop Engineering (20%) · **16.0 / 20**

| 기준 | 레벨 | 근거 | 감점 사유 |
|---|---|---|---|
| L1 파이프라인 | **3** | 볼트 Inbox→Raw→Wiki→Project→Output + 명령. AX-Workflow 8-Phase(산출물 `phase:` 필드 실사용). 생산↔기억 담당 명시 | 단계별 체류 시간·전환율 측정 없음 |
| L2 주기 리뷰 | **4** | 최근 30일 Work List 30/31일(09-11 누락), 팀 회고 12건(08-24~09-21), 주간 운영 로그 연속, DAIOS 주간·월간. 회고 → 변경 사례: 07-20 KPT Try → active.md "1클릭 대기" 신설, 발화 2,735턴 분석 → 룰 개정 | 회고 로그 **파싱 실패 2건** |
| L3 적체·흐름 | **2** | 주간 archive-candidates·drift-report, 클리핑 가드, Claudian-Ingest-Watch 자동화 | 볼트 `10_Inbox` 35건이 **어떤 주기 리뷰에서도 추적되지 않음.** 프로젝트 쪽은 stale 80/128로 적체가 누적 중 |
| L4 복리 환류 | **3** | 주간 산출 루프 커밋(08-30·09-06·09-20), DAE-VaultOutput, `publish_to_vault.py`, 볼트 재사용 검색을 작업 순서에 편입(도달 4→19건) | 환류율 지표 없음. 산출물이 "draft, 정훈님 손질 대기"로 반복 정체. 4→19는 경로 오타 수리 효과라 환류 파이프라인 개정으로 보기 어려움 |
| L5 자기교정 | **4** | 감사(07-13, 4에이전트) → 재감사(09-06) 2사이클. 라우팅 룰은 개정 → 실측 실패 → 훅 이관 → 재개정 3회차(테스트 11/11·5/5) | **권고 이행률 추적표 없음.** 07-19 보안 감사의 시크릿 로테이션 4건은 완료 기록을 찾지 못함 |

### X — Interop & Governance (15%) · **9.0 / 15**

| 기준 | 레벨 | 근거 | 감점 사유 |
|---|---|---|---|
| X1 런타임 이식성 | **3** | GEMINI.md·Codex AGENTS.md, 크로스AI 팩 5종 오늘 자동 갱신, `AX-SHARED-SKILL-INDEX.md`가 런타임별 기능 차이 문서화. Emil 실행 검증 1건(09-20) → v1.1 각주 요건 충족 | **Eva·Emil 자동 실행 0건** — 자동화가 전부 Max(이 PC). 런타임 간 드리프트 점검 없음 |
| X2 기기 토폴로지 | **2** | `cross-device-remote.md`(3계층: Victus·MacBook·폰), 캐릭터 태그로 작성자 표기 | 가이드가 **v0.1 draft**. MacBook 설치 3단계 잔여. 에이전트별 출력 폴더 강제 없음. 충돌 기록은 07-30 사고 1건뿐 |
| X3 복구 가능성 | **2** | 볼트 git(최근 09-20), 야간 robocopy(`D:\00 Backup`), 노션 원문 백업 러너북 | **오프사이트 복사본이 없음** — 볼트 git에 원격이 없고, robocopy는 같은 D: 드라이브 안. `~/.claude`·Antigravity 전체가 VCS 밖(평가 시점). 복원 리허설 기록 1건(07-19)뿐 |
| X4 보안 | **2** | 99개 레포 보안 감사(33개 수정), `scan_exposed_keys.ps1`, 백업 시 자동 `[REDACTED]`, 토큰 로테이션 러너북 | 시크릿 관리 도구 없이 `.env`·평문 자격증명 파일. 에이전트의 민감 경로 접근 제한(deny) 0. 로테이션 4건 추적 없음. 정기 감사 없음 |
| X5 전수·표준화 | **3** | FLOW 스튜디오 카탈로그 278건 git→Vercel 주간 배포, 옵시디언 발표 덱 26장, 기업 강의 실활용(09-15~09-17 3건 등) | D.A.E 자체를 버전 관리되는 **스타터 킷으로 릴리스한 적 없음.** 채택자 피드백 → 킷 개정 기록 없음 |

### 합계

| 필러 | 가중치 | 레벨 합 | 점수 |
|---|---|---|---|
| P Prompt | 20% | 16 / 20 | **16.0** |
| C Context | 25% | 14 / 20 | **17.5** |
| H Harness | 20% | 16 / 20 | **16.0** |
| L Loop | 20% | 16 / 20 | **16.0** |
| X Interop | 15% | 12 / 20 | **9.0** |
| **총점** | | | **74.5 → M3 오케스트레이션** |

N/A 처리 없음(25/25 채점). 레벨 4는 6개(P2·P3·C5·H5·L2·L5)로, 모두 "시스템이 시스템을 고친 날짜 있는 기록"을 근거로 했습니다.

---

## 2. 상위 강점 3

1. **규칙을 코드로 강제하는 습관** (P2·P3·H3) — 문서로 반복 실패한 것을 훅으로 옮겼습니다: `route-agents.js`·`dgrs-gate.js`·`verify-before-done.js`·`guard-destructive.js`. 루브릭이 레벨 3·4를 가르는 핵심("어기기가 지키기보다 어려움")을 여러 곳에서 충족합니다.
2. **실패를 실측하고 되먹이는 루프** (L5·H5) — "에이전트 사용률 14%→10%" 같은 불리한 수치를 스스로 측정해 룰 실패를 인정하고 구조를 바꾼 기록이 날짜와 함께 3회차까지 이어집니다. 1인 시스템에서 드문 수준입니다.
3. **운영 리듬의 실가동** (L2) — 최근 30일 Work List 30/31, 주간 운영 로그 연속, 팀 회고 12건. 설계만 있는 시스템이 아니라 매일 도는 시스템입니다.

## 3. 최우선 개선 5 (예상 점수 상승폭)

| # | 개선 | 대상 기준 | 예상 상승 | 비고 |
|---|---|---|---|---|
| 1 | **오프사이트 백업** — 볼트 git과 `claude-config` 스냅샷에 비공개 원격(GitHub private 등) 추가 + 분기 1회 복원 리허설 | X3 2→3(→4) | **+0.75~1.5** | 원격에 올리기 전 시크릿 스캔 필수. 볼트의 `80-SECURE/`는 제외 설계 필요 → **정훈님 결정 D1** |
| 2 | ✅ **에이전트용 하이브리드 검색** — `vault_search.py` + `vault_embed.py` + 스킬 `vault-search`(§5) | C3 2→3 | **+1.25** | **완료(2026-09-23)** — `qmd`는 Homebrew 전용이라 포기하고, 이미 있던 Smart Connections 임베딩을 CLI로 노출하는 쪽을 택함 |
| 3 | **볼트 주소체계 단일화 지도** — PARA 8 + LLM-Wiki 7을 볼트 CLAUDE.md 한 표로 설명하고, 루트의 무제 파일 격리 | C1 2→3 | **+1.25** | 이동이 아니라 "지도 작성 + 격리"(볼트 운영 룰 §4 준수) |
| 4 | ✅ **볼트 인박스를 주간 운영에 편입** — `run_weekly_ops.bat` step8(§5) | L3 2→3 | **+1.0** | **완료(2026-09-22)** — 다음 주간 리뷰부터 추적 시작, 레벨 상향은 추적 기록이 쌓인 뒤 재평가 |
| 5 | **민감 경로 deny + 주간 키 스캔 스케줄** — settings `deny`에 자격증명 파일 경로, `scan_exposed_keys.ps1`을 주간 운영에 연결, 로테이션 4건 추적표 | X4 2→3 · H3 3→4 | **+1.75** | deny 추가는 권한 설정 변경 → **정훈님 결정 D2** |

1~5를 모두 반영하면 **약 81~82점(M4 경계)**으로 예상합니다 `[추정]`.

## 4. 감점 뿌리 추적표 (§8.3 권장)

| 뿌리 설계 선택 | 파생 감점 | 한 번에 끊는 조치 |
|---|---|---|
| **모든 자동화·설정이 PC 1대의 D: 드라이브에 집중** | X3(오프사이트 없음) · X1(Eva/Emil 자동 실행 0) · X2(MacBook 미완) | 개선 #1 — 원격 백업을 먼저 하면 X2·X1 확장의 전제가 생김 |
| **설정 정본(`~/.claude`)을 VCS 밖에 둠** | P5 · X3 | **§5에서 조치 완료**(로컬 스냅샷 + 주간 자동 커밋) — 남은 것은 원격 |
| **볼트에 외부 킷(스타터킷 v1.2)을 기존 PARA 위에 덧댐** | C1 · C3 · L3 | 개선 #2·#3·#4 — 지도·검색·인박스 추적을 볼트 한 곳에 맞춤 |
| **권한 확인을 끄고 가드 훅 하나로 방어** (의도된 속도 선택) | H3 4점 불가 · X4 | 개선 #5 — 속도는 유지하고 deny 목록만 얇게 추가 |

## 5. 평가 중 즉시 조치한 것 (점수 미반영)

- **`~/.claude` 설정 스냅샷 repo 신설** — `D:/00 Backup/claude-config.git` (git-dir 분리 방식이라 `~/.claude` 안에 `.git`이 생기지 않음).
  - 허용목록: rules · scripts · agents · commands · settings.json · AGENTS.md → **246개 파일**. 자격증명·세션·캐시·`settings.local.json`·스킬 2,529개는 제외.
  - 커밋 전 시크릿 패턴 스캔 0건, 추적 파일명 중 자격증명류 0건.
  - `run_weekly_ops.bat`에 **step7**을 추가해 매주 월 08:30 자동 커밋. 격리 테스트 2회로 "no changes" / "committed" 분기 확인.
- **볼트 인박스 주간 추적** — `run_weekly_ops.bat`에 **step8**을 추가해 `10_Inbox` 파일 수를 주간 sync 리포트에 기록합니다(목표 20건 미만). 격리 테스트 결과 34건으로, bash 교차검증 34건과 일치합니다.
- 반영 시: **P5 2→3 (+1.0) → 총점 75.5**. X3는 같은 드라이브 안이라 **변동 없음**(개선 #1이 남은 이유). L3는 추적 기록이 1회 이상 쌓인 다음 평가에서 재판정합니다(루브릭 L 필러 30일 가동 규칙).
- **에이전트용 볼트 하이브리드 검색 신설** — `scripts/vault_search.py`(BM25, 의존성 0) + `scripts/vault_embed.py`(질의 임베딩) + 스킬 `vault-search`.
  - 볼트에 이미 있던 Smart Connections 임베딩(3,572개 · `TaylorAI/bge-micro-v2` 384d)을 그대로 재사용합니다. 같은 모델로 질의만 임베딩하면 같은 공간에서 비교됩니다.
  - 정합성 검증: 노트 제목을 질의로 넣는 자가검색에서 6건 중 5건이 1위(mean/CLS 풀링 모두 동일) — 벡터 정렬이 맞다는 증거.
  - 융합 가중치는 정답 노트 3건의 순위로 실측해 lex:vec = 3:1로 정했습니다(표는 스킬 문서 §2). 서술형 질의에서 17위 → 2위로 올라가는 것이 융합을 쓰는 이유입니다.
  - 인덱스 3,236 노트 30초 · 하이브리드 질의 2초. 임베더가 실패하면 자동으로 렉시컬로 폴백합니다(검증함).
  - `80-SECURE/`는 색인 제외.
- `[미검증]` 전체 배치(`run_weekly_ops.bat`) 통합 실행 — step7·8은 격리 실행으로만 검증했습니다. 첫 실전은 다음 월요일 08:30 `AX-WeeklyOps`입니다.
- `[미검증]` C3 레벨 상향(2→3)은 **다음 평가에서 판정**합니다. 루브릭 레벨 3은 "지식 기반의 80%+ 커버 + 에이전트 노출"을 요구하는데, 커버리지(3,236 노트 중 벡터 3,572개)는 충족하나 실사용 기록이 아직 없습니다.

## 6. 런타임 매트릭스 (§9)

| 런타임 | 컨텍스트 연결 | 스킬/자동화 | 역할 | 가동 | 방향 |
|---|---|---|---|---|---|
| Claude Code (Max) | 정본 직결 | 있음(233 + 훅 21) | 주력 | 일상 | 유지 |
| Antigravity (Eva) | 어댑터(GEMINI.md + eva 팩) | 없음(자동 실행 0) | 특화(기획·이미지) | 간헐 | 확대 — 팩 1회 실행 검증 로그 남기기 |
| ChatGPT·Codex (Emil) | 어댑터(AGENTS.md + emil/codex 팩, agents 32) | 없음 | 특화(카피·판정) | 간헐 | 유지 |
| Claudian (볼트 상주) | 정본 직결(볼트) | 있음(`/ingest`·`/query`·`/lint`) | 주력(기억층) | 일상 | 유지 |

## 7. 정훈님 결정 필요

| ID | 결정 | 소요 | 이것이 풀어주는 것 | 권고 |
|---|---|---|---|---|
| D1 | 볼트 git + `claude-config`를 **GitHub private repo로 원격 백업** | 1분(한마디) | X3 오프사이트 복구, 뿌리 1 해소 | ✅ **완료 (9/22)** — 볼트·설정 모두 **비공개** 원격에 올림. 볼트는 80-SECURE·재무·제3자 연락처 제외 후 3,245파일, 설정은 옛 이력 격리 후 새 이력 246파일. 둘 다 주간 배치에서 올리기 전 비밀값 검사, 발견 시 중단(종료코드 3) |
| D2 | settings `deny`에 **자격증명 파일 경로 추가**(`.credentials.json`·`oauth_creds.json`·`.env`) | 1분 | X4·H3 상향, 에이전트의 민감 경로 접근 차단 | ✅ **완료 (9/22)** — deny 6개(`//**/` 전역 형식). 읽기·cat 차단 실측. 허용 목록 속 비밀값 5건도 제거 |

개선 #2·#4는 완료했습니다. #3(볼트 지도·무제 파일 격리)은 볼트 운영 룰 §5에 따라 구조 변경 전 커밋 확인과 승인이 필요해 **제안으로 남깁니다.**

## HISTORY
- 2026-09-22 D1·D2 완료 반영 — 옵시디언 운영 재설계 세션에서 실행. (Max)
- 2026-09-22 v1.0 최초 자가진단 — Explore 에이전트 증거 수집(25기준·예약 작업 46·훅 37) + Max 채점(보수적·기준별 강제 감점). 평가 중 `~/.claude` 스냅샷 repo 신설(점수 미반영). CMDSPACE 미제출. (Max)
